from django.apps import AppConfig


class FamilyFundsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "family_funds"
    verbose_name = "Family Funds (private, per-family)"
